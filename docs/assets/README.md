# Documentation assets

Place real application screenshots here using the filenames listed in `../presentation-guide.md`.

Recommended first asset: `03-verify-result.png`, because the verification result best communicates the project's engineering depth.
